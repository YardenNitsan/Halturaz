node_modules/
dist/
smoke/dist/
*.local
.DS_Store
.env
.env.*
# ...but the templates carry no secrets and belong in the repo.
!.env.example
!.env.admin.example
# The published build's client-side settings; see the file's own header.
!.env.production
npm-debug.log*
yarn-error.log*

# Generated design canvas bundle (2.6 MB); reseed it from design/ instead.
rehearsal-manager.html

# Generated smoke audit screenshots
app/smoke/phone/

# Database dumps taken before destructive test runs
app/db/backups/

# Generated Edge Function bundle; rebuild it with `npm run fn:build`.
app/supabase/functions/import/index.ts

# Scratch space the Supabase CLI writes next to the config.
app/supabase/.temp/

# Vercel CLI scratch
.vercel/
